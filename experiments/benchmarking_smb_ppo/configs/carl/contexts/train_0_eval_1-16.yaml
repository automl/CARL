# @package _global_

contexts:
  per_context_features:
    noise: [0]
  constant_features: ${constant_features}

eval_contexts:
  per_context_features:
    noise: range_1_17
  constant_features: ${constant_features}