# @package _global_

contexts:
  per_context_features:
    noise: range_0_16
  constant_features: ${constant_features}

eval_contexts:
  per_context_features:
    noise: range_0_16
  constant_features: ${constant_features}