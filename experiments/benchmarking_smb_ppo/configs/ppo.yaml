defaults:
  - _self_
  - base
  - slurm/gpu
  - env/mario_base
  - carl/context_sampling/base
  - carl/context_visibility/visible_changing
  - carl/contexts/base

restore: null

total_timesteps: 200_000_000
num_steps: 400

optimizer:
  lr: 0.0001

num_minibatches: 8
update_epochs: 3

gamma: 0.99
gae_lambda: 0.95
normalize_advantage: true
clip_ratio: 0.2
clip_value_loss: true
entropy_weight: 0.01
value_weight: 0.5
max_grad_norm: 40
target_kl_div: null

batch_size: ???
minibatch_size: ???
device: ???

env: ???
eval_env: ???