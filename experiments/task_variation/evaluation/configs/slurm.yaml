defaults:
  - override hydra/launcher: submitit_slurm
hydra:
  launcher:
    partition: ${env:HYDRA_SLURM_PARTITION,cpu_short}
    mem_gb: 1
    cpus_per_task: 1
    timeout_min: 720
  job:
    name: evPol