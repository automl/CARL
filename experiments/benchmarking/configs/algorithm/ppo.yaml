# @package _global_

algorithm: ppo

# Hyperparameters from here: https://github.com/araffin/rl-baselines-zoo/blob/master/hyperparams/ppo2.yml
learning_rate: 1e-3
clip_lr: 1e-4
gamma: 0.9
replay_capacity: 512
pi_target_tau: 0.1
entropy_regularizer_beta: 0.01
batch_size: 32

network:
  width: 8
