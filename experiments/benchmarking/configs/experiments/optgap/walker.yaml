# @package _global_
defaults:
  - ../../environments/dmc/walker

context_sampler:
  env_name: ${env}
  context_feature_names: ["viscosity"]
  seed: ${seed}
  n_samples: 64
  # sigma_rel: 0.25  # 0.1, 0.25, 0.5
  uniform_distribution: true
  uniform_bounds_rel: [1, 2.5]