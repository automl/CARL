# @package _global_
defaults:
  - ../../environments/classic_control/pendulum

context_sampler:
  env_name: ${env}
  context_feature_names: ["l"]
  seed: ${seed}
  n_samples: 64
  # sigma_rel: 0.25  # 0.1, 0.25, 0.5
  uniform_distribution: true
  uniform_bounds_rel: [1, 2.5]