# @package _global_

landing_in_space:
  sample_function:
    _target_: experiments.policy_transfer.landing_in_space.definitions.sample_gravities_normal
    mean: -3.69710705  # gravity of Mars
    std: 1.45
    seed: ${seed}
    n_contexts: ${context_sampler.n_samples}