# @package _global_
defaults:
  - ../environments/classic_control/pendulum
  - ../context_visibility/hidden
  - ../slurm/cpushort
  - ../algorithm/td3

experiment: optimality_gap2

carl:
  context_mask: null

eval_on_train_context: true

context_sampler:
  env_name: ${env}
  context_feature_names: ["l"]
  seed: ${seed}
  n_samples: 10
  sigma_rel: 0.25  # 0.1, 0.25, 0.5

# # SAC
# alpha: 0.1