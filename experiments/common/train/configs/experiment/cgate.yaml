# @package _global_
wandb:
  group: cgate_sb3
carl:
  use_cgate: true

agent:
  kwargs:
    policy:
      _target_: experiments.common.train.policies.cgate.get_cgate_policy
      agent_name: ${agent.name}
    policy_kwargs:
      context_branch_width: 512
      head_width: ${agent.kwargs.policy_kwargs.context_branch_width}